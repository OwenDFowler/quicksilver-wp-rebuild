[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.wordpress"

[deploy]
startCommand = "quicksilver-wordpress-start apache2-foreground"
healthcheckPath = "/wp-json/"
healthcheckTimeout = 300
